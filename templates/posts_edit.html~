{% extends 'base.html' %}

{% block titre %}
<title>Edition de message</title>
{% endblock %}

{% block body %}
    <h1>Mise à jour du message :</h1>
    <form action="/posts/edit/{{ post['id'] }}" method="POST">
        <input class="form-control" type="text" name="title" id="title" value="{{ post['title'] }}"  placeholder="Titre : {{ post['title'] }}">
        <br>
        <input class="form-control" type="text" name="author" id="author" value="{{ post['author'] }}" placeholder="Auteur : {{ post['author'] }}">
        <br>
        <textarea class="form-control" name="content" id="content" placeholder="{{ post['content'] }}">{{ post['content'] }}</textarea>
        <br>
        <input type="submit" value="Sauver" class="btn btn-success">
    </form>
    <hr>
    <form action="/carte/new" method="POST">
        <textarea class="form-control" type="text" name="question" id="question" placeholder="Question : {{ card['question'] }}" required>{{ card['question'] }}</textarea>
        <br>
        <textarea class="form-control" type="text" name="answer" id="answer" placeholder="Réponse : {{ card['answer'] }}" required>{{ card['answer'] }}</textarea>
        <br>
        <input class="form-control" list="listcourse" name="course" id="course" value="{{ card['course'] }}"  placeholder="cours : {{ card['course'] }}" required>
			<datalist id="listcourse">	
			{% for course in courselist %}
			  <option value="{{ course['course'] }}">
			{% endfor %}
			</datalist>
        <br>
        <input type="submit" value="Envoyer" class="btn btn-success">
    </form>

{% endblock %}

